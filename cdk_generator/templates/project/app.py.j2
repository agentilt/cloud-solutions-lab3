{# CDK app entry point for the GENERATED user-facing project. #}
{# Rendered once per project. Vars: project_name, stack_class, services #}
{# TODO(diego): emit `#!/usr/bin/env python3`, imports, App(), Stack instantiation, app.synth() #}
