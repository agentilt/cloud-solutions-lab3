# {{ project_name }}

Generated by CloudCompass Builder from a governed architecture specification.

## Services

{% for service in services -%}
- `{{ service.logical_id }}`: `{{ service.type }}`
{% endfor %}

## Quickstart

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
cdk synth
```

The project is account-agnostic. It does not hardcode an AWS account, region,
or ARN. Review the synthesized template and CloudFormation change set before
deployment.
